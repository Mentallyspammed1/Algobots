"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var data_forge_1 = require("data-forge");
function direction(period) {
    if (period === void 0) { period = 2; }
    chai_1.assert.isNumber(period, "Expected 'period' parameter to 'Series.direction' to be a number that specifies the time period for the direction test.");
    return this.rollingWindow(2)
        .select(function (window) { return [
        window.getIndex().last(),
        Math.sign(window.last() - window.first()),
    ]; })
        .withIndex(function (pair) { return pair[0]; })
        .select(function (pair) { return pair[1]; });
}
data_forge_1.Series.prototype.direction = direction;
//# sourceMappingURL=direction.js.map