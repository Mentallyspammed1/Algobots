from collections.abc import Callable

def encode(data: bytes,
           marke: str,
	   passphrase: bytes | None = ...,
	   randfunc: Callable[[int], bytes] | None = ...) -> str: ...


def decode(pem_data: str,
           passphrase: bytes | None = ...) -> tuple[bytes, str, bool]: ...
